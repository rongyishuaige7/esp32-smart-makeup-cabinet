#ifndef SMART_MAKEUP_CABINET_WIFI_CREDENTIALS_EXAMPLE_H
#define SMART_MAKEUP_CABINET_WIFI_CREDENTIALS_EXAMPLE_H

// Copy the declarations below into the Git-ignored config.local.h only for a
// personally supervised, low-voltage bench test on a trusted local network.
// Never commit that local file. Keeping all values below empty/0 preserves the
// public default: no Wi-Fi, no HTTP, no physical actuator command.

/*
#define WIFI_SSID ""
#define WIFI_PASSWORD ""
#define ENABLE_EXPERIMENTAL_ACTUATORS 0
#define ENABLE_EXPERIMENTAL_LOCAL_CONTROL 0
#define ENABLE_EXPERIMENTAL_RFID_MAINTENANCE 0
*/

#endif
